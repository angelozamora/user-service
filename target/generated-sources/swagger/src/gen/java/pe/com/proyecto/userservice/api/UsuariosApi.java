package pe.com.proyecto.userservice.api;

import pe.com.proyecto.userservice.api.models.Usuario;
import pe.com.proyecto.userservice.api.models.Usuarios;

import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.Map;
import javax.ws.rs.*;
import javax.ws.rs.core.Response;
import javax.ws.rs.core.MediaType;
import org.apache.cxf.jaxrs.ext.multipart.*;

import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import io.swagger.annotations.ApiResponses;
import io.swagger.annotations.ApiResponse;
import io.swagger.jaxrs.PATCH;
import javax.validation.constraints.*;
import javax.validation.Valid;

/**
 * user-service.yaml
 *
 * <p>No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 */
@Path("/")
@Api(value = "/", description = "")
public interface UsuariosApi  {

    @GET
    @Path("/user/usuarios")
    @ApiOperation(value = "", tags={ "Usuarios",  })
    @ApiResponses(value = { 
        @ApiResponse(code = 200, message = "Operacion correcta", response = Usuarios.class),
        @ApiResponse(code = 400, message = "Datos de entrada incorrectos", response = Usuarios.class) })
    public Usuarios userUsuariosGet();

    @POST
    @Path("/user/usuarios")
    @ApiOperation(value = "", tags={ "Usuarios",  })
    @ApiResponses(value = { 
        @ApiResponse(code = 200, message = "Operacion correcta", response = Usuarios.class),
        @ApiResponse(code = 400, message = "Datos de entrada incorrectos", response = Usuarios.class) })
    public Usuarios userUsuariosPost(@Valid Usuario body);

    @DELETE
    @Path("/user/usuarios/{usuarioid}")
    @ApiOperation(value = "", tags={ "Usuarios",  })
    @ApiResponses(value = { 
        @ApiResponse(code = 200, message = "Operacion correcta", response = Usuarios.class),
        @ApiResponse(code = 400, message = "Datos de entrada incorrectos", response = Usuarios.class) })
    public Usuarios userUsuariosUsuarioidDelete(@PathParam("usuarioid") String usuarioid);

    @GET
    @Path("/user/usuarios/{usuarioid}")
    @ApiOperation(value = "", tags={ "Usuarios",  })
    @ApiResponses(value = { 
        @ApiResponse(code = 200, message = "Operacion correcta", response = Usuarios.class),
        @ApiResponse(code = 400, message = "Datos de entrada incorrectos", response = Usuarios.class) })
    public Usuarios userUsuariosUsuarioidGet(@PathParam("usuarioid") String usuarioid);

    @GET
    @Path("/user/usuarios/validarusuario/user/{user}/password/{password}")
    @ApiOperation(value = "", tags={ "Usuarios" })
    @ApiResponses(value = { 
        @ApiResponse(code = 200, message = "Operacion correcta", response = Usuarios.class),
        @ApiResponse(code = 400, message = "Datos de entrada incorrectos", response = Usuarios.class) })
    public Usuarios userUsuariosValidarusuarioUserUserPasswordPasswordGet(@PathParam("user") String user, @PathParam("password") String password);
}

